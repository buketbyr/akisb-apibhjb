# Akış Bilinci — observations-app (Final)

- Next.js 13.4.4 + TS + Tailwind + Prisma + Postgres
- Guard’lı build: DATABASE_URL yoksa db push atlanır.
- UI: /, /new, /logs — API: /api/observations, /api/health
